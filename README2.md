Data221 Assignment 1

1. problem1 – Counts the number of vowels in a given string and prints the result.
2. problem2 – Creates a dictionary for each word in a list with its string length and whether the length is even or odd.
3. problem3 – Defines a function to compute x^y, loops over a list of (x, y) pairs, skips negative exponents, and prints valid results.
4. problem4 – Sorts a list of random values, finds indices where values are greater than or equal to a random x, and prints the sorted list and first matching index.
5. problem5 – Compares areas of two circles after validating positive integer radii, and returns the percentage coverage of the smaller circle over the larger one.
6. problem6 – Analyzes a list of numbers and returns a dictionary of each unique value with the percentage of elements less than or equal to it, sorted by key.
7. problem7 – Converts a number of seconds since midnight into hours, minutes, seconds, and AM/PM, returning a formatted string.
8. problem8 – Creates a pandas DataFrame from given data, adds a computed column based on existing columns, and prints the final DataFrame.


- All code runs without errors.
